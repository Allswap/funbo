{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "Hardhat deployment scripts for ArbExecutor contract (deactivated: bot swaps direct wallet->router; kept for reference/redeployment)",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network polygon",
    "deploy:testnet": "hardhat run scripts/deploy.js --network amoy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "dotenv": "^17.4.2",
    "hardhat": "^3.16.0"
  },
  "overrides": {
    "mocha": {
      "serialize-javascript": "^7.1.1",
      "diff": "^8.0.3"
    }
  }
}
