{
  "name": "hardhat",
  "version": "1.0.0",
  "description": "Hardhat deployment scripts for ArbExecutor contract",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network polygon",
    "deploy:testnet": "hardhat run scripts/deploy.js --network amoy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6"
  },
  "type": "module"
}
